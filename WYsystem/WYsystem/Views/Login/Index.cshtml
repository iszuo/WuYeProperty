<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1">
    <link href="~/Content/assets/css/bootstrap-clearmin.min.css" rel="stylesheet" />
    <link href="~/Content/assets/css/roboto.css" rel="stylesheet" />
    <link href="~/Content/assets/css/font-awesome.min.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <title>物业管理系统</title>
    <style></style>
</head>
<body class="cm-login">

    <div class="text-center" style="padding:90px 0 30px 0;background:#fff;border-bottom:1px solid #ddd">
        <h1>物业管理系统</h1>
    </div>

    <div class="col-sm-6 col-md-4 col-lg-3" style="margin:40px auto; float:none;">
        <form method="post" id="myForm" onsubmit="return checkForm()" action="/Login/Index">
            <div class="col-xs-12">
                <div class="form-group">
                    <div class="input-group">
                        <div class="input-group-addon"><i class="fa fa-fw fa-user"></i></div>
                        <input type="text" name="username" id="userName" maxlength="16" class="form-control" placeholder="用户名">
                    </div>
                </div>
                <div class="form-group">
                    <div class="input-group">
                        <div class="input-group-addon"><i class="fa fa-fw fa-lock"></i></div>
                        <input type="password" name="password" id="userPwd" maxlength="10" class="form-control" placeholder="密码">
                    </div>
                </div>
            </div>
            <div class="col-xs-6">
                <div class="checkbox"><label><input type="checkbox">记住我</label></div>
            </div><div class="col-xs-6">

                <button type="submit" class="btn btn-block btn-primary">登录</button>
            </div>
            <h1 class="col-lg-12" id="notice" style="color:red;">@ViewBag.notice</h1>
        </form>
    </div>
    <script>
        function checkForm() {
            var userName = $("#userName").val();
            var userPwd = $("#userPwd").val();
            if (userName.length < 1) {
                $("#notice").text("用户名不能为空！");
                return false;       // 不让表达往后执行
            }
            if (userPwd.length < 1) {
                $("#notice").text("密码不能为空！");
                return false;       // 不让表达往后执行
            }
        }
    </script>
</body>
</html>
